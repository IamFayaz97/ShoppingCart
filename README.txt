<--------------README FOR SHOPPING CART APPLICATION-------------->

1)	I have implemented Swagger Config for easier documentation and presentation of APIs.
	Link for Swagger is : http://localhost:8080/swagger-ui.html#/ (port is set at default 8080).

2)	I have used In-memory database(h2) for data storing.
	Link to view the h2 database console: http://localhost:8080/h2-console/ (port is set at default 8080).
	Credentials for h2 database console: User Name = "user", Password = "user"